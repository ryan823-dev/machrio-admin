# Railway Project Configuration
# Backend Service

[build]
# Use Docker for building
dockerfile = "backend/machrio-api/Dockerfile"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
numReplicas = 1
